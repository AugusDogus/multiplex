# Implementation Plans

Generated by the improve skill on 2026-07-15; **reconciled 2026-07-16**
against `origin/main` @ `43e847c`.

Execute remaining TODO plans in the order below. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

## What changed on main since the first pass

| Landed | Effect on backlog |
|--------|-------------------|
| `da1a10b` CI workflow (`.github/workflows/ci.yml`) | **001 done independently** |
| `6173202` plex-query healthy server client reuse | Partial browse/server latency win; **002 still needed** (home hydrate/`staleTime: 0`/CW poll unchanged) |
| Playlists management (feat + fixes) | New product surface; not on the original todo list |
| Live TV guide reload out of read path + refresh UX | Guide path improved; **clicks still stubbed** — 005 more urgent, not less |
| Auth hardening, image proxy, media-player progress cleanup | Orthogonal; does not change direction ranking |

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add GitHub Actions CI for check + unit tests | P1 | S | — | REJECTED — fixed independently (`da1a10b` / `.github/workflows/ci.yml`) |
| 002 | Spike: make browse as fast as Plex web on current stack | P1 | M | — | TODO (refreshed @ `43e847c`) |
| 003 | Polish `/login` to match in-app Multiplex brand | P2 | S–M | — | TODO (refreshed @ `43e847c`) |
| 004 | Spike: View Transitions on poster → item details | P2 | M | — | TODO (refreshed @ `43e847c`) |
| 005 | Ship-or-hide Live TV guide until tune exists | P1 | S | — | TODO (refreshed @ `43e847c`; elevated — guide invested, playback still missing) |

Pre-existing (not numbered, not from this pass):

| Doc | Status |
|-----|--------|
| `plans/effect-v4-migration.md` | Phases 0–4 + PlayerService done; Phase 5 server-data migration deferred |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- **002 is the top executable bet** for “as fast as Plex web.” Do not start a sync engine.
- **005 rose to P1**: main kept polishing Live TV guide while program/channel clicks remain `console.log` TODOs. Hide/flag before more guide work, or commit to a separate tune spike (not in this backlog).
- 003 and 004 are independent polish tracks; either can parallelize with 002.
- PR preview Docker/Railway is **still parked**, but no longer blocked on “no CI.” Still needs a hosting target + secrets model (plex.tv egress, auth).

## Findings considered and rejected (park)

- **Zero / Electric / TanStack DB sync engine for Plex catalog** — premature. Catalog SoT is remote PMS/plex.tv; SQLite is auth only; AGENTS.md + Effect migration Phase 5 keep tRPC + TanStack Query as the Plex data plane. Exhaust 002 first.
- **Client-side WASM/ffmpeg transcoding** — not worth doing. Playback already uses Plex universal remux/transcode + `<video src>`; a second client decode path fights session accounting and Watch Together cost.
- **Extract `packages/media-player`** — wait until a second consumer (Expo/TV) exists. Player is entangled with Next, tRPC, Effect atoms, and WT UI.
- **Overseerr / Jellyseerr built-in** — zero repo signal; park until WT users repeatedly hit “missing title” friction.
- **PR preview deploys (Docker/Railway)** — wait until there is a real hosting target + secrets model. CI gate now exists; previews are the next infra step *after* that decision, not before browse/login polish.
- **Expo / PWA / Android TV / Roku / desktop native** — premature platform fan-out. Zero seeds in-repo; harden mobile Safari/web first if platform work is desired.
- **001 CI baseline** — rejected as a plan because main already shipped it.

## Maintainer todo → recommendation map (post-reconcile)

| Your item | Verdict now |
|-----------|-------------|
| Faster than / as fast as Plex web (+ sync engine?) | **Do next** via 002; client reuse helped serverside; hydrate/poll still the gap; **not** a sync engine |
| PR preview Docker/Railway | Unblocked by CI; still wait for hosting/secrets — not ahead of 002/003 |
| Client-side transcoding feasibility | Park / reject as product path |
| View transitions | **Do** via 004 (poster→details first) |
| Sign-in page polish | **Do** via 003 |
| Overseerr support | Park |
| Live TV again | **Decide now** via 005 — stop polishing a dead-end guide, or fund tune |
| Split packages (video player) | Wait for second consumer |
| Mobile / PWA / TV / Roku / desktop | Wait; invest in mobile web quality instead |

## Executable right now

1. `plans/002-browse-speed-current-stack.md`
2. `plans/005-live-tv-ship-or-hide.md` (quick product decision)
3. `plans/003-login-brand-polish.md`
4. `plans/004-view-transitions-poster-details.md`
