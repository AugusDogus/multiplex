# Implementation Plans

Generated by the improve skill on 2026-07-15 (`next` / roadmap pass over the
maintainer's todo list). Default selection (non-interactive): top leverage items
that compound — CI unlock, browse-speed on the existing stack, login polish,
poster→details view transitions, Live TV ship-or-hide.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add GitHub Actions CI for check + unit tests | P1 | S | — | TODO |
| 002 | Spike: make browse as fast as Plex web on current stack | P1 | M | — | TODO |
| 003 | Polish `/login` to match in-app Multiplex brand | P2 | S–M | — | TODO |
| 004 | Spike: View Transitions on poster → item details | P2 | M | — | TODO |
| 005 | Ship-or-hide Live TV guide until tune exists | P2 | S | — | TODO |

Pre-existing (not numbered, not from this pass):

| Doc | Status |
|-----|--------|
| `plans/effect-v4-migration.md` | Phases 0–4 + PlayerService done; Phase 5 server-data migration deferred |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- 001 does not block 002–005, but should land first so later PRs get a green gate.
- 002 and 004 are independent spikes; do not start a sync-engine project as part of 002.
- 005 is a product decision (hide vs finish). Prefer hide/feature-flag unless you are committing to a Live TV playback spike separately.

## Findings considered and rejected (park)

Recorded so they are not re-audited as “missing work”:

- **Zero / Electric / TanStack DB sync engine for Plex catalog** — premature. Catalog SoT is remote PMS/plex.tv; SQLite is auth only; AGENTS.md + Effect migration Phase 5 keep tRPC + TanStack Query as the Plex data plane. Exhaust 002 first.
- **Client-side WASM/ffmpeg transcoding** — not worth doing. Playback already uses Plex universal remux/transcode + `<video src>`; a second client decode path fights session accounting and Watch Together cost.
- **Extract `packages/media-player`** — wait until a second consumer (Expo/TV) exists. Player is entangled with Next, tRPC, Effect atoms, and WT UI.
- **Overseerr / Jellyseerr built-in** — zero repo signal; park until WT users repeatedly hit “missing title” friction.
- **PR preview deploys (Docker/Railway)** — wait until there is a real hosting target + secrets model. Useful previews need plex.tv egress and auth; do 001 first.
- **Expo / PWA / Android TV / Roku / desktop native** — premature platform fan-out. Zero seeds in-repo; README “any browser” already overclaims. Harden mobile Safari / web first if platform work is desired.

## Maintainer todo → recommendation map

| Your item | Verdict |
|-----------|---------|
| Faster than / as fast as Plex web (+ sync engine?) | **Do next** via 002; **not** via a sync engine yet |
| PR preview Docker/Railway | After 001 + a real deploy target |
| Client-side transcoding feasibility | Park / reject as product path; optional write-up only |
| View transitions | **Do** via 004 (poster→details first) |
| Sign-in page polish | **Do** via 003 |
| Overseerr support | Park |
| Live TV again | **Decide** via 005 (hide until tune, or commit to playback) |
| Split packages (video player) | Wait for second consumer |
| Mobile / PWA / TV / Roku / desktop | Wait; invest in mobile web quality instead |
