# Multiplex for Wii

This app is the Wii composition root. It combines the shared
`@multiplex/console-ui` interface with the shared `@multiplex/libogc-gx`
runtime and selects Wii libogc rules, WPAD input, and Wii networking.

Build it from the repository root with `bun run wii:reference:dol`.
