diff --git a/tcp_conn.h b/tcp_conn.h
index 77a04a0..115626d 100644
--- a/tcp_conn.h
+++ b/tcp_conn.h
@@ -57,7 +57,7 @@ struct tcp_tap_conn {
 #define RTT_EXP_BITS			4
 	unsigned int	rtt_exp		:RTT_EXP_BITS;
 #define RTT_EXP_MAX			MAX_FROM_BITS(RTT_EXP_BITS)
-#define RTT_STORE_MIN			100 /* us, minimum representable */
+#define RTT_STORE_MIN			20000 /* us, paced Dolphin BBA minimum */
 #define RTT_STORE_MAX			((long)(RTT_STORE_MIN << RTT_EXP_MAX))
 #define RTT_SET(conn, rtt)						\
 	(conn->rtt_exp = MIN(RTT_EXP_MAX,				\
diff --git a/tcp.c b/tcp.c
index 81d041b..45a95a4 100644
--- a/tcp.c
+++ b/tcp.c
@@ -1149 +1149,2 @@ int tcp_update_seqack_wnd(const struct ctx *c, struct tcp_tap_conn *conn,
-	if (bytes_acked_cap && delivery_rate_cap && !force_seq &&
+	if (c->mode != MODE_PASTA &&
+	    bytes_acked_cap && delivery_rate_cap && !force_seq &&
@@ -1209,0 +1211,2 @@ int tcp_update_seqack_wnd(const struct ctx *c, struct tcp_tap_conn *conn,
+	if (c->mode == MODE_PASTA)
+		new_wnd_to_tap = MAX(new_wnd_to_tap, WINDOW_DEFAULT);
@@ -1330,0 +1335,2 @@ int tcp_prepare_flags(const struct ctx *c, struct tcp_tap_conn *conn,
+		if (c->mode == MODE_PASTA)
+			conn->ws_to_tap = 0;
diff --git a/tap.c b/tap.c
index 2a6a288..1185431 100644
--- a/tap.c
+++ b/tap.c
@@ -15,0 +16 @@
+#include <time.h>
@@ -426 +426 @@ static size_t tap_send_frames_pasta(const struct ctx *c,
- * #syscalls:pasta write
+ * #syscalls:pasta write nanosleep clock_nanosleep
@@ -452,0 +453,5 @@ static size_t tap_send_frames_pasta(const struct ctx *c,
+		if (i + bufs_per_frame < nbufs) {
+			const struct timespec delay = { .tv_nsec = 100 * 1000 };
+
+			nanosleep(&delay, NULL);
+		}
